var oldAnswer = $('.favourite-answer').contents();

oldAnswer.appendTo('.answers');
$('.answer').filter(function(){
    return $(this).data('answerId') === <%= @answer.id %>
}).remove();

$('.favourite-answer').html("<%= j render @answer %>");
